name: 'Chromatic Deployment'

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: yarn
      - uses: chromaui/checkout@v1
        with:
          projectToken: ef5af38aec3a
          token: ${{ secrets.GITHUB_TOKEN }}
